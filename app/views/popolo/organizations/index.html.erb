<% @organizations.group_by(&:classification).each do |classification,organizations| %>
<%= render partial: 'classification', locals: {classification: classification, organizations: organizations} %>
<% end %>
